# Jira MCP Server Configuration
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# Then edit .env with your actual values

# ============================================================================
# REQUIRED SETTINGS
# ============================================================================

# Your Jira instance URL (without trailing slash)
# For self-hosted Jira only (Cloud Jira is not supported)
# Example: https://jira.yourcompany.com
JIRA_MCP_URL=https://jira.yourcompany.com

# Your Jira API token for authentication
# How to get: Log into Jira → Profile → Personal Access Tokens → Create Token
# Example: ATBBxxx...xxxyyy
JIRA_MCP_TOKEN=your-api-token-here

# ============================================================================
# OPTIONAL SETTINGS (with defaults)
# ============================================================================

# Schema cache time-to-live in seconds
# Default: 3600 (1 hour)
# Caching reduces API calls by 80%+ for repeated schema lookups
# Set to 0 to disable caching (useful for development)
JIRA_MCP_CACHE_TTL=3600

# HTTP request timeout in seconds
# Default: 30
# Increase if your Jira instance is slow or has large responses
# Decrease for faster failure detection
JIRA_MCP_TIMEOUT=30
